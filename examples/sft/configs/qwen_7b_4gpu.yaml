# Qwen 2.5 7B — 4x H100 Data Parallel Configuration

model:
  name: "/media/volume/Model/hf_cache/models--Qwen--Qwen2.5-7B/snapshots/d149729398750b98c0af14eb82c78cfe92750796"
  dtype: "bfloat16"
  devices: [0, 1, 2, 3]
  attn_implementation: "flash_attention_2"
  trust_remote_code: true

dataset:
  name: "gsm8k"
  dataset_dir: "data"
  max_seq_len: 1024
  num_workers: 4

training:
  batch_size: 632
  gradient_accumulation_steps: 1
  num_steps: 10
  learning_rate: 1.0e-5
  weight_decay: 0.01
  max_grad_norm: 1.0
  seed: 42

optimizer:
  type: "deepspeed_adam"
  beta1: 0.9
  beta2: 0.999
  eps: 1.0e-8

memory:
  checkpoint_interval: 4
  num_grad_slabs: 12

logging:
  log_interval: 1
  enable_timing: true
